<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only overlay.

  The :advisor server runs over plain HTTP during local development
  (http://10.0.2.2:8080 from the emulator), and Android blocks cleartext
  traffic by default. This flag lifts that block for DEBUG builds ONLY - the
  release manifest is untouched, so a shipped build still refuses cleartext and
  must talk to the advisor over HTTPS.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic" />

</manifest>
